spring:
  application:
    name: EurekaRigstryCenterApplication
server:
  port: 8083

eureka:
  instance:
    hostname: localhost
  client:
#    registerWithEureka: false
#    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8082/eureka/
